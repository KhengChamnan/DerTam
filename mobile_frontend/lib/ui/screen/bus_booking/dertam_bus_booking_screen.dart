import 'package:flutter/material.dart';

class DertamBusBookingScreen extends StatelessWidget {
  const DertamBusBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}